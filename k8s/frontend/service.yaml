apiVersion: v1
kind: Service
metadata:
  name: hirequest-frontend
  namespace: hirequest
spec:
  type: NodePort
  selector:
    app: hirequest-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
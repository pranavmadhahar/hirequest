services:
  frontend:
    image: ghcr.io/pranavmadhahar/hirequest/frontend:latest
    pull_policy: if_not_present
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    image: ghcr.io/pranavmadhahar/hirequest/backend:latest
    pull_policy: if_not_present
    ports:
      - "8000:8000"
    volumes:
      - ./data/db:/data
      - ./data/vectorstores:/data/vectorstores
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    depends_on:
      - assets

  assets:
    image: ghcr.io/pranavmadhahar/hirequest/assets:latest
    pull_policy: if_not_present
    volumes:
      - ./data/vectorstores:/app/src/assets/vectorstores
      - ./data/raw_data:/app/src/assets/raw_data
      - ./data/cleaned_data:/app/src/assets/cleaned_data
      - ./data/chunks:/app/src/assets/chunks
    # command removed – assets now works directly with host vectorstores
